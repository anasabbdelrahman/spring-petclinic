detect-secrets pre-commit hook - blocking evidence
Date: 2026-09-23
Branch: homework/security-defense-in-depth
HEAD at test time: 4641ced (Add vet pagination migration PR description)

Tooling
- pre-commit 4.6.2, detect-secrets 1.5.0 (user-local venv ~/.local/share/precommit-tools)
- Hook: https://github.com/Yelp/detect-secrets rev v1.5.0, id detect-secrets,
  args --baseline .secrets.baseline

Test
- Temporary file: tmp-detect-secrets-canary.txt
- Content: a comment line, then on line 2 a keyword assignment holding a fake,
  test-only value. Value: [REDACTED]. It was never a real credential and was
  never added to .secrets.baseline.
- Staged: only tmp-detect-secrets-canary.txt (git diff --cached --name-only).

Command attempted
  git commit -m "TEST: detect-secrets canary (must be blocked)"

Result
- git commit exit status: 1 (nonzero)
- Hook output (verbatim apart from the redaction, which is not needed here
  because the hook does not print the value):
    Detect secrets...........................................................Failed
    - hook id: detect-secrets
    - exit code: 1
    ERROR: Potential secrets about to be committed to git repo!
    Secret Type: Secret Keyword
    Location:    tmp-detect-secrets-canary.txt:2
- Hook name: detect-secrets
- Flagged location: tmp-detect-secrets-canary.txt, line 2
- The hook was not bypassed (no --no-verify, no SKIP, no allowlist pragma).

Cleanup and confirmation
- No commit was created: HEAD was 4641ced before and after the attempt.
- The file was unstaged (git restore --staged) and deleted.
- The fake value is absent from the working tree, the index, every ref's
  history (git log --all -S), the stash and .secrets.baseline.
- git add had written one unreachable loose blob holding the file; that single
  object was deleted. No git prune or gc was run, so other unreachable objects
  were left untouched. git fsck reported no errors afterwards.
- .secrets.baseline was byte-identical before and after the hook run.

Baseline audit - for the later security report
- Finding: k8s/db.yml:14, Secret Keyword, the password of the demo-db
  Kubernetes Secret (value [REDACTED]; a trivial placeholder paired with a
  placeholder username on line 13).
- Audit: detect-secrets audit .secrets.baseline, answered "n" to "Should this
  string be committed to the repository?", which records is_secret: true.
- Classification: confirmed pre-existing weak demo credential. It predates
  this branch and was committed upstream.
- Disposition: accepted temporarily. This task adds detection; it does not
  change deployment configuration. k8s/db.yml is unchanged, and the value is
  not allowlisted (no pragma comment and no exclude). It is tracked only as an
  audited baseline entry, so the hook reports new secrets but not this one.
- Remediation recommendation: inject the database credential from a
  deployment-managed secret (for example one created by the deployment
  pipeline or a secrets manager, never from the repository) and remove the
  committed password from k8s/db.yml. Then rotate the credential in every
  environment that used it, and regenerate the baseline so the entry drops out.
